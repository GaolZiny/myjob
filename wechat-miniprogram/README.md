# 微信小程序 - 新闻展示方案

这是一个完整的微信小程序云开发方案，用于从你的 API 服务器同步新闻数据到微信云数据库，并在小程序中展示。

## 架构说明

```
API服务器 (PostgreSQL)
    ↓
    ↓ 每小时自动同步
    ↓
微信云函数 (syncNews)
    ↓
    ↓ 保存数据
    ↓
微信云数据库 (news集合)
    ↓
    ↓ 读取数据
    ↓
微信小程序 (前端展示)
```

**核心优势**：
- ✅ 小程序直接读取云数据库，速度快
- ✅ 不依赖外部API，稳定性高
- ✅ 自动去重，避免数据重复
- ✅ 每小时自动同步最新新闻
- ✅ 支持按分类筛选、分页加载

## 文件结构

```
wechat-miniprogram/
├── cloudfunctions/              # 云函数目录
│   └── syncNews/                # 同步新闻的云函数
│       ├── index.js             # 云函数主代码
│       ├── package.json         # 依赖配置
│       └── config.json          # 定时触发器配置
│
├── miniprogram/                 # 小程序前端目录
│   ├── pages/
│   │   └── news/                # 新闻列表页
│   │       ├── news.js          # 页面逻辑
│   │       ├── news.wxml        # 页面结构
│   │       ├── news.wxss        # 页面样式
│   │       └── news.json        # 页面配置
│   │
│   └── app.js                   # 小程序入口文件
│
├── database-collections.md      # 云数据库集合设计文档
└── README.md                    # 本文件
```

## 快速开始

### 步骤 1：开通微信云开发

1. 在微信开发者工具中打开你的小程序项目
2. 点击顶部菜单：**云开发** → **开通**
3. 创建一个新的云环境（如：news-prod）
4. 等待环境初始化完成（约1-2分钟）

### 步骤 2：创建云数据库集合

1. 打开微信开发者工具
2. 点击 **云开发控制台**
3. 选择 **数据库** 标签
4. 点击 **创建集合**
5. 集合名称输入：`news`
6. 点击确定

#### 配置数据库索引（重要！）

在 `news` 集合的 **索引管理** 中创建以下索引：

| 索引名称 | 字段名 | 类型 | 升序/降序 |
|---------|--------|------|-----------|
| link_unique | link | 唯一索引 | 升序 |
| category_index | category | 普通索引 | 升序 |
| created_at_index | created_at | 普通索引 | 降序 |
| source_index | source | 普通索引 | 升序 |

#### 设置数据库权限

在 `news` 集合的 **权限设置** 中：

```json
{
  "read": true,
  "write": false
}
```

### 步骤 3：部署云函数

1. 在微信开发者工具中，右键点击 `cloudfunctions/syncNews` 文件夹
2. 选择 **上传并部署：云端安装依赖**
3. 等待部署完成（约30秒-1分钟）

**重要**：修改 `cloudfunctions/syncNews/index.js` 第 11 行，填入你的实际 API 地址：

```javascript
const API_BASE_URL = 'https://your-api-domain.com'; // 修改为你的API地址
```

### 步骤 4：配置定时触发器

云函数部署成功后，定时触发器会自动创建：

1. 打开微信云开发控制台
2. 选择 **云函数** 标签
3. 找到 `syncNews` 云函数
4. 点击 **定时触发器** 标签
5. 确认触发器 `hourlySync` 已创建
   - **触发周期**：`0 0 * * * * *`（每小时整点触发）
   - **状态**：已启用

**Cron 表达式说明**：
```
0 0 * * * * *
│ │ │ │ │ │ │
│ │ │ │ │ │ └─ 年（可选）
│ │ │ │ │ └─── 星期（0-7，0和7都表示周日）
│ │ │ │ └───── 月（1-12）
│ │ │ └─────── 日（1-31）
│ │ └───────── 时（0-23）
│ └─────────── 分（0-59）
└───────────── 秒（0-59）
```

示例：
- 每小时：`0 0 * * * * *`
- 每30分钟：`0 0,30 * * * * *`
- 每天凌晨2点：`0 0 2 * * * *`

### 步骤 5：手动触发首次同步

1. 打开微信云开发控制台
2. 选择 **云函数** → **syncNews**
3. 点击 **云端测试** 标签
4. 点击 **运行测试**
5. 查看日志，确认同步成功

**预期结果**：
```json
{
  "success": true,
  "message": "同步成功！新增 50 条，更新 0 条，失败 0 条，耗时 3200ms",
  "syncTime": "2025-11-10T12:00:00.000Z",
  "stats": {
    "total": 50,
    "new": 50,
    "updated": 0,
    "failed": 0
  }
}
```

### 步骤 6：在小程序中添加新闻页面

#### 方法 A：复制示例代码（推荐）

将 `miniprogram/pages/news/` 目录下的文件复制到你的小程序项目中。

#### 方法 B：在现有页面中集成

在你的页面 JS 文件中添加：

```javascript
async loadNews() {
  const db = wx.cloud.database();
  const result = await db.collection('news')
    .orderBy('created_at', 'desc')
    .limit(20)
    .get();

  this.setData({
    newsList: result.data
  });
}
```

#### 注册页面路由

在 `app.json` 中添加页面路径：

```json
{
  "pages": [
    "pages/news/news",
    "pages/index/index"
  ]
}
```

### 步骤 7：初始化云环境

在 `app.js` 中初始化云开发：

```javascript
App({
  onLaunch() {
    // 初始化云开发环境
    if (!wx.cloud) {
      console.error('请使用 2.2.3 或以上的基础库以使用云能力');
    } else {
      wx.cloud.init({
        env: 'your-env-id',  // 替换为你的云环境ID
        traceUser: true
      });
    }
  }
});
```

**获取云环境ID**：
1. 打开微信云开发控制台
2. 点击左上角的环境名称
3. 复制 **环境ID**（如：news-prod-xxxxx）

### 步骤 8：测试小程序

1. 在微信开发者工具中编译小程序
2. 打开 **新闻** 页面
3. 下拉刷新加载数据
4. 点击分类标签筛选新闻
5. 上拉加载更多

## 常见问题

### 1. 云函数执行失败：`cloud.cloud.HTTPClient is not a function`

**原因**：wx-server-sdk 版本过低

**解决方案**：
```bash
# 在云函数目录下执行
cd cloudfunctions/syncNews
npm install wx-server-sdk@latest
```

然后重新上传云函数。

### 2. 数据库查询报错：`permission denied`

**原因**：数据库权限设置错误

**解决方案**：
1. 打开云开发控制台 → 数据库 → news集合
2. 点击 **权限设置**
3. 将 `read` 设置为 `true`

### 3. 云函数请求API超时

**原因**：API服务器响应慢或网络问题

**解决方案**：
修改 `cloudfunctions/syncNews/index.js` 第 121 行：

```javascript
timeout: 30000,  // 增加到30秒
```

### 4. 定时触发器不执行

**原因**：触发器未启用或配置错误

**解决方案**：
1. 打开云开发控制台 → 云函数 → syncNews → 定时触发器
2. 确认触发器状态为 **已启用**
3. 如果没有触发器，点击 **新建触发器**：
   - 名称：hourlySync
   - 类型：定时触发器
   - 触发周期：`0 0 * * * * *`

### 5. 小程序获取不到数据

**检查清单**：
1. 云函数是否执行成功？（查看云函数日志）
2. 云数据库是否有数据？（打开数据库控制台查看）
3. 云环境ID是否正确？（检查 app.js）
4. 数据库权限是否正确？（read: true）

**调试方法**：
```javascript
// 在小程序页面中添加
async testCloudDB() {
  const db = wx.cloud.database();
  const result = await db.collection('news').count();
  console.log('数据库中新闻数量:', result.total);
}
```

## API 接口说明

### 云函数使用的API接口

云函数调用你的API服务器的以下接口：

#### GET /api/news/latest

获取最新新闻列表

**请求参数**：
- `page` (Number)：页码，默认 1
- `pageSize` (Number)：每页数量，默认 100

**响应格式**：
```json
{
  "success": true,
  "data": [
    {
      "id": 123,
      "title": "AI Breakthrough in 2025",
      "title_zh": "2025年人工智能取得重大突破",
      "link": "https://...",
      "summary_zh": "...",
      "category": "科技",
      "keywords": "AI,technology",
      "source": "BBC News",
      "image_url": "https://...",
      "pub_date": "2025-11-10T10:00:00Z",
      "created_at": "2025-11-10T11:05:00Z"
    }
  ],
  "pagination": {
    "page": 1,
    "pageSize": 100,
    "total": 500
  }
}
```

## 性能优化建议

### 1. 数据库优化

**定期清理旧数据**（创建 cleanOldNews 云函数）：

```javascript
// cloudfunctions/cleanOldNews/index.js
const cloud = require('wx-server-sdk');
cloud.init();
const db = cloud.database();
const _ = db.command;

exports.main = async (event, context) => {
  const thirtyDaysAgo = new Date(Date.now() - 30 * 24 * 60 * 60 * 1000);

  const result = await db.collection('news')
    .where({
      created_at: _.lt(thirtyDaysAgo)
    })
    .remove();

  return {
    success: true,
    removed: result.removed
  };
};
```

定时触发器设置为每天凌晨2点：`0 0 2 * * * *`

### 2. 小程序端优化

**使用虚拟列表**（数据量大时）：
- 安装 `weui-miniprogram` 组件库
- 使用 `recycle-view` 组件

**图片懒加载**：
```xml
<image lazy-load="{{true}}" src="{{item.image_url}}"></image>
```

**分页优化**：
```javascript
// 使用 skip 和 limit 实现高效分页
db.collection('news')
  .skip(page * pageSize)
  .limit(pageSize)
  .get();
```

### 3. 云函数优化

**批量操作**：
```javascript
// 使用批量写入代替单条插入
const tasks = newItems.map(item =>
  db.collection('news').add({ data: item })
);
await Promise.all(tasks);
```

## 成本估算

### 微信云开发免费配额

- **云数据库**：2GB 存储，5GB/天 读取，3GB/天 写入
- **云函数**：1000 GBs/月 资源使用量
- **云存储**：5GB 容量，10GB/月 下载，5GB/月 上传

### 预估使用量

假设每小时同步50条新闻：
- **云数据库存储**：每条新闻约 1KB，50条/小时 × 24小时 × 30天 = 36MB/月
- **云函数调用**：24次/天 × 30天 = 720次/月
- **云数据库读取**：小程序端读取，取决于用户量

**结论**：在合理使用下，免费配额完全够用！

## 进阶功能

### 1. 添加搜索功能

```javascript
// 全文搜索
db.collection('news')
  .where({
    title_zh: db.RegExp({
      regexp: keyword,
      options: 'i'
    })
  })
  .get();
```

### 2. 添加收藏功能

创建新集合 `favorites`：
```javascript
// 收藏新闻
db.collection('favorites').add({
  data: {
    news_id: newsId,
    user_id: userId,
    created_at: db.serverDate()
  }
});
```

### 3. 消息推送

使用云函数 + 订阅消息推送重要新闻：
```javascript
await cloud.openapi.subscribeMessage.send({
  touser: openid,
  page: 'pages/news-detail/news-detail',
  data: {
    thing1: { value: newsTitle },
    thing2: { value: newsSummary }
  },
  templateId: 'your-template-id'
});
```

## 技术支持

如有问题，请查看：
- [微信云开发文档](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/basis/getting-started.html)
- [数据库指南](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/guide/database.html)
- [云函数指南](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/guide/functions.html)

## 总结

现在你已经拥有一个完整的新闻小程序解决方案：

✅ **自动化**：每小时自动同步最新新闻
✅ **高性能**：小程序直接读取云数据库，速度快
✅ **低成本**：使用微信云开发免费配额
✅ **易维护**：云函数自动去重，无需手动管理
✅ **可扩展**：支持搜索、收藏、推送等功能

开始使用吧！🚀
